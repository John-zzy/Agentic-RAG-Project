同步 README.md 和 AGENTS.md 文档至最新，注意文档篇幅，能合并就合并，能拆分文档就挂索引，文档创建到 [documents](docs/documents/) ，包括流程图也挪到对应的 documents 中文档分模块分架构。

## 要求：
1. 当前 @docs/ 下的那么多 mermaid 架构图归类、以及 api-list、data-model 都归类到 documents 文件夹中维护；
2. 核心目的让 llm 能够更快的找到对应的文档，渐进明细的获取对应的内容、Agents.md 主要服务 AI Agent，用于快速建立代码导航、理解架构边界、找到改动入口并避免常见误改；
3. README.md 文档是给用户看的，当前项目的核心功能、后续目标计划、项目截图、架构以及如何运行等，核心是推广项目使用；
3. 当前项目高频错误、常见的坑需要写入一个文档中；